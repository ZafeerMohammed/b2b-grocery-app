spring.application.name=grocery

# PostgreSQL Database Configuration
spring.datasource.url = jdbc:postgresql://localhost:5432/grocerydb
spring.datasource.username = postgres
spring.datasource.password = Postgres@101

# JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Dev Tools (optional restart on change)
spring.devtools.restart.enabled=true

# Spring Security
#spring.security.user.name=aaa
#spring.security.user.password=aaa
#spring.security.enabled=false
